---
name: anything-roadmap
description: 为任意领域生成系统化学习路线（模块→主题→章节→知识点四级大纲）。先真实调研前人学习路线作骨架，再为每个知识点搜索并逐条验证权威源（官方文档/博客/视频/网站，英文权威为骨、中文教学为辅），产出 Markdown + 自包含交互 HTML 双格式。当用户想为某领域制定学习大纲、学习路线、系统学习规划、入门路线、curriculum、roadmap、syllabus、learning path 时使用。不做：把已有书籍/课程蒸馏成 skill；生成人物思维 skill。
---

# anything-roadmap — 任意领域学习路线生成器

**调用方式**：`/anything-roadmap <领域> [一句话背景]`

**示例**：
- `/anything-roadmap Rust编程`（默认零基础）
- `/anything-roadmap 分布式系统 有五年后端开发经验`
- `/anything-roadmap 量子计算 物理学本科背景`

**产出物**：在当前目录生成 `./<领域>-roadmap-<YYYYMMDD>/` 文件夹，包含：
- `syllabus.md` — 完整学习路线（四级大纲 + 验证过的源）
- `index.html` — 可交互的知识星图（单文件，零依赖，离线可开）

## 铁律（违反任何一条即失败）

1. **零幻觉链接**：进入成品的每一个 URL 都必须真实打开验证过——链接活着、内容对题、出处确实权威。验证不过就不放，宁缺毋滥。
2. **前人路线为骨**：主题树来自搜到的现成学习路线的合成，不是凭模型记忆现编。可信度阶梯：官方路线 > 大学课表/知名课程目录 > 高星 awesome-list > 高信噪比个人路线文 > 社区讨论共识。搜不到 ≥2 条可信路线 → 降级为"官方文档目录结构为骨"，并在成品的生成说明中显式声明。
3. **一站到底**：消歧之后不再向用户提问。用户事后给纠正意见 = 带纠正意见全新重跑全流程，生成新日期目录，绝不修补旧稿。
4. **预算**：每知识点 ≤3 个源、全份封顶 ~40（同一 URL 可挂多个知识点，只占 1 个名额）。超预算时收窄主题树，不是放宽上限。

## 流程

### 0. 消歧（最多两问，问完不再问）
- 领域一句话消歧，如"支付"→支付系统开发还是支付业务？用户答"都行"→ 取最主流解释，并把该假设写进成品"生成说明"。
- 若 args 未带背景，可再问一句"有相关基础吗？"；跳过或含糊 → 零基础。

### 1. 探测搜索通道（先复用已装 skill，再降级）

**通道探测策略**：
1. 检查环境中已安装的搜索类 skill（如 agent-reach）
2. 若可用，先执行健康检查（如 `agent-reach doctor --json`）确认今天哪些通道真的活着
3. 按活着的通道路由任务；全部失败或未安装 → 降级到内置工具
4. 编程库/框架类领域：优先使用 context7 获取官方文档（可用时用，非硬依赖）

**搜索路由表**：

| 任务类型 | 首选工具 | 命令示例 | 降级方案 | 失败处理 |
|---------|---------|---------|---------|---------|
| 前人路线/官方文档/博客/网站 | Exa | `mcporter call exa.web_search_exa query="…" numResults=5` | WebSearch | 记录失败，尝试降级 |
| awesome-list / GitHub 仓库 | gh CLI | `gh search repos "…" --sort stars --limit 10` | WebSearch | 未认证时自动降级 |
| YouTube 视频 | yt-dlp | `yt-dlp --dump-json "ytsearch5:…"` | WebSearch | 解析失败用 python 处理 |
| B站视频 | bili-cli | `PYTHONUTF8=1 bili search "…" --type video -n 5` | WebSearch | 防 Windows GBK 乱码 |
| 验证链接（三通道接力） | Jina Reader | `curl -s "https://r.jina.ai/<URL>"` | web_reader MCP → WebFetch | curl 失败≠死链，换通道重试 |

**重要注意事项**：
- 本地网络抖动常见，curl/直连失败不等于链接已死，必须换通道重试后再判定
- jq 缺失时，yt-dlp 可用 `--flat-playlist --print` + python 脚本解析
- Windows 环境特别注意 GBK 编码问题，必要时添加 `PYTHONUTF8=1`
- 并行搜索时控制并发数，避免触发 API 限流

### 2. 前人路线调研

**核心目标**：找到 ≥2 条可信的现成学习路线作为大纲骨架

**可信度阶梯**（从高到低）：
1. **官方路线** — 技术官方网站的 Learning Path、Getting Started Guide
2. **大学课表/知名课程** — MIT/Stanford 等大学课程大纲、Coursera/edX 课程结构
3. **高星 awesome-list** — GitHub 上 stars >1000 的精选资源列表
4. **高信噪比个人路线** — 知名从业者/技术博主的系统性学习指南
5. **社区讨论共识** — Reddit/知乎/掘金上高赞的学习路径讨论

**搜索策略**：
- **优先查找 llms.txt**：现代文档站的机器可读全站索引，一次获取全部页面清单
  - 示例：`curl <domain>/llms.txt` 或 `<domain>/.well-known/llms.txt`
  - 包含全部页面 + 一句话描述，极大提升效率
- **中英双语搜索**：
  - 英文：`<domain> roadmap`、`how to learn <domain>`、`<domain> syllabus site:.edu`、`awesome <domain>`
  - 中文：`<领域> 学习路线`、`<领域> 入门 site:zhihu.com`、`<领域> 系统学习 site:juejin.cn`
- **社区间接搜索**：`how to learn <domain> site:reddit.com`（经 Exa 等工具间接获取）

**验证流程**：
1. 每条候选路线必须真实打开链接确认存在且内容对口
2. 记录：tier（可信度级别）/ 标题 / URL / 一句话点评
3. 存入 `priorPaths` 数组供后续引用

**降级模式触发条件**：
- 搜索结果 < 2 条可信路线 → 切换为"官方文档目录结构为骨"模式
- 必须在成品的"生成说明"部分显式声明使用了降级模式
- 降级时优先使用官方文档的 llms.txt 或 sitemap 作为结构参考

**特殊领域处理**：
- **冷门/新兴领域**：更依赖官方文档 + GitHub 仓库 README + 社区早期讨论
- **交叉学科**：分别搜索各子领域的路线，再合成
- **中文特色领域**：加大中文源权重，B站视频课程、尚硅谷等机构课程可作为课表层前人路线

### 3. 合成主题树
- 按知识内聚拆**模块→主题→章节→知识点**四级；模块编号 M1..Mn 体现学习顺序。
- 每模块记 why（为何这么切）与 refs（支撑它的前人路线序号）；记模块间关联（from/to/note）。
- 控制规模：知识点总数要装得下"每点 ≤3 源、全份 ~40"的预算。

### 4. 搜源
每个知识点按阶梯选 ≤3 个候选：官方文档 > 权威书/课 > 深度博客 > 视频。英文权威为骨、中文教学为辅（视频与入门讲解优先 B站/知乎/掘金）。媒介混搭（doc/blog/video/site）。

### 5. 逐条验证（三通道接力 + 三问准入）

**验证目标**：确保成品中每个链接都真实可访问、内容对题、出处权威

**三通道接力策略**：
1. **Jina Reader**（首选）：`curl -s "https://r.jina.ai/<URL>"`
   - 优点：返回纯文本内容，易于检查
   - 失败时 → 通道 2
2. **web_reader MCP**（次选）：通过 MCP 协议读取网页
   - 优点：本地 MCP 服务更稳定
   - 失败时 → 通道 3
3. **WebFetch**（兜底）：内置工具直接抓取
   - 最后防线

**重要规则**：
- **curl/直连失败 ≠ 死链**：本地网络抖动、代理问题常见，必须换通道重试
- 三个通道全部失败才判定为死链
- 记录每次验证的通道和结果，便于调试

**三问准入机制**（全部通过才放行）：

| 检查项 | 判定标准 | 常见拒绝原因 |
|--------|---------|-------------|
| **1. 活着？** | HTTP 状态 200-299，内容非空 | 404/403/超时/重定向到错误页 |
| **2. 对题？** | 内容包含领域关键词，标题与主题相关 | SEO 聚合站、同名异义内容、营销软文 |
| **3. 权威？** | 官方站/知名作者/高信誉平台/学术出版 | 个人低质博客、AI 生成内容农场、过时教程 |

**特殊情况处理**：
- **仿冒站识别**：检查域名、About 页面、内容生成痕迹（如 Hermes 案例：hermes-agent.org 伪装官方）
- **版本漂移**：博客评测若写于旧版本，必须对照当前官方文档校准结论有效性
- **同名异义**：如 Hermes（AI Agent / Meta JS 引擎 / 机械臂），需确认是目标领域
- **重定向处理**：跟踪重定向，但最终落地页必须仍通过三问
- **语言混杂**：英文权威源为主，中文教学源为辅，比例约 7:3

**并行优化**：
- 候选源较多（>20）时，可使用并行子代理加速验证
- 每个子代理负责一组源，独立执行三问检查
- 汇总结果时统一计数：found / passed / dropped

**验证统计**（必须在成品结尾展示）：
```
验证统计：搜到 X 个候选 / 通过 Y 个 / 剔除 Z 个
剔除原因分布：死链 A 个 / 不对题 B 个 / 不够权威 C 个
```

### 6. 成稿与交付

**输出目录结构**：`./<领域>-roadmap-<YYYYMMDD>/`

**必须生成的文件**：

#### 1. syllabus.md（完整学习路线）
按以下骨架组织全部内容，正文中文、来源标题保留原文：

```markdown
# <领域> 学习路线
> 生成日期：<YYYY-MM-DD> | 制图模式：<前人路线为骨 / 官方文档回退> | 受众：<基础假设> | 验证：搜到 X / 通过 Y / 剔除 Z

## 0 全局地图
### 0.1 领域面貌
<对该领域整体的描述，核心问题是什么，为什么重要>

### 0.2 模块拆分依据
<为什么按这个方式划分模块，按什么逻辑内聚>

### 0.3 模块间关联
- **M1 ⇄ M2**：<模块 1 和模块 2 如何关联，学习顺序为何>
- **M2 ⇄ M3**：<关联说明>

## M1 <模块名>
> **为何这么切**：<此模块的设计理由>  
> **参考路线**：[R1] [R2]

### 1.1 <主题名>
#### 1.1.1 <章节名>
- **<知识点名称>**
  - [DOC·EN Title](url) — <一句话说明>
  - [VID·中 标题](url) — <一句话说明>
  - [BLOG·EN Title](url) — <一句话说明>

### 1.X 勘测练习
1. <动手练习 1>
2. <动手练习 2>

## M2 <模块名>
...

## 附录 A 参考图源（前人路线）
1. **[官方]** [标题](url) — <一句话点评>
2. **[课表]** [标题](url) — <一句话点评>
3. **[社区]** [标题](url) — <一句话点评>

## 附录 B 生成说明
本学习路线由 **anything-roadmap** 于 <日期> 生成，制图模式：**<模式>**。

**验证统计**：共检索 X 个候选来源，逐条打开验证通过 Y 个、剔除 Z 个（死链/不对题/不够权威）。

**受众假设**：<基础水平描述>。

**制图模式说明**：<若为降级模式，此处声明原因>

**纠错说明**：本路线为一次性快照。若需纠正，请带纠正意见重新调用生成全新版本，不对旧版修补。
```

#### 2. index.html（交互式知识星图）

**生成步骤**：
1. 复制 `references/template.html` 到目标目录
2. **只修改 DATA 对象**：找到 `/* DATA-START */` 和 `/* DATA-END */` 两个标记
3. 替换之间的 `const DATA = {...}` 对象（保留标记本身）
4. 其余 HTML/CSS/JS 代码一律不动

**DATA 对象 Schema**：
```javascript
const DATA = {
  domain: "领域名称",
  date: "YYYY-MM-DD",
  mode: "prior" | "docs",  // prior=前人路线为骨, docs=官方文档回退
  level: "零基础" | "有X经验",
  stats: { found: 数字, passed: 数字, dropped: 数字 },
  overview: {
    landscape: "领域面貌描述",
    cohesion: "模块拆分依据"
  },
  relations: [
    { from: 模块序号, to: 模块序号, note: "关联说明" }
  ],
  modules: [
    {
      name: "模块名",
      why: "为何这么切",
      refs: [0, 1],  // 参考的 priorPaths 索引
      topics: [
        {
          name: "主题名",
          chapters: [
            {
              name: "章节名",
              points: [
                {
                  name: "知识点",
                  sources: [
                    { type: "doc|blog|video|site", lang: "EN|中", title: "标题", url: "链接", note: "说明" }
                  ]
                }
              ]
            }
          ]
        }
      ],
      exercises: ["练习1", "练习2"]
    }
  ],
  priorPaths: [
    { tier: "官方|课表|社区", title: "标题", url: "链接", comment: "点评" }
  ]
};
```

**零 CDN 自检**（生成后必须执行）：
```bash
grep -E 'src="http|link[^>]*href="http' index.html
```
- 必须返回空（除了 DATA 内的 `<a>` 标签）
- 禁止引入任何外部 script / stylesheet / 字体 / 图片
- 确保完全离线可开

#### 3. 交付检查清单

生成结束后必须完成：

- [ ] 在对话中明确列出两个产物的**完整绝对路径**
  - 示例：`D:\Workspace\<领域>-roadmap-<日期>\syllabus.md`
  - 示例：`D:\Workspace\<领域>-roadmap-<日期>\index.html`
- [ ] 把两个文件发送给用户（使用文件发送功能）
- [ ] 确认 index.html 通过零 CDN 检查
- [ ] 验证统计数字准确（found = passed + dropped）

**纠正重跑处理**：
- 用户事后提出纠正意见 = 触发全新重跑
- 将纠正意见作为硬约束，从第 0 步（消歧）重新执行
- 生成新的日期目录，绝不修改旧版文件
- 在新版的"生成说明"中注明"本版修正了：<用户纠正内容>"

## 边界与限制

**本 skill 专注的范围**：
- ✅ 为任意领域制定系统化学习路线（含冷门/新兴/交叉学科）
- ✅ 调研并合成前人的学习路线设计
- ✅ 搜索并验证权威学习资源（文档/博客/视频/网站）
- ✅ 生成可交互的知识地图可视化

**明确不做的事情**（属于其他工具职责）：
- ❌ 把已有书籍/课程内容蒸馏成 skill（使用 cangjie 等专门工具）
- ❌ 生成特定人物的思维模式 skill（使用 nuwa 等专门工具）
- ❌ 提供实时学习进度跟踪或打卡系统
- ❌ 生成详细的时间表或学习计划（只给顺序和依赖关系）
- ❌ 替代真实的课程学习或导师指导

**无工具环境的替代方案**：
- 网页版 Claude、ChatGPT 等无工具调用能力的环境
- 使用导出的方法论：`references/playbook.md`
- 整段复制提示词给对方 AI 使用
- **注意**：无工具环境无法做到 100% 逐条验证链接，需人工抽查

## 常见问题处理

### Q1：搜索通道全部失败怎么办？
**A**：按优先级依次尝试：
1. 检查本地网络和代理配置
2. 尝试降级工具（Exa → WebSearch）
3. 若仍失败，使用模型知识 + 显式声明"本次搜索受限，部分内容基于训练数据"
4. 在成品中标注哪些源未能验证

### Q2：领域过于冷门，搜不到任何路线？
**A**：触发降级模式：
1. 搜索该领域的官方文档/主仓库
2. 以官方文档目录结构为骨架组织大纲
3. 在"生成说明"显式声明：该领域未找到 ≥2 条可信前人路线，已采用官方文档结构
4. 加大社区讨论权重（Reddit/知乎早期探索性讨论）

### Q3：用户提出纠正意见后怎么办？
**A**：绝不修补旧稿，而是全新重跑：
1. 将纠正意见记录为本次的硬约束
2. 从第 0 步（消歧）重新执行全部流程
3. 生成新的日期目录（如 `<领域>-roadmap-20260822`）
4. 在新版"生成说明"注明"本版修正了：<具体内容>"

### Q4：如何处理同名异义领域？
**A**：在消歧阶段处理：
1. 识别该名称的多个可能含义（如 Hermes：AI Agent / JS 引擎 / 机械臂）
2. 向用户询问具体指哪个方向
3. 若用户答"都行"→ 选最主流解释，并在"生成说明"记录该假设
4. 搜索和验证时明确过滤其他含义的内容

### Q5：验证时遇到大量死链怎么办？
**A**：
1. 先确认是否是通道问题（换通道重试）
2. 若确实大量失效，扩大搜索范围找替代源
3. 记录详细的剔除统计（死链/不对题/不权威各多少）
4. 若某个知识点实在找不到 3 个源，允许 1-2 个，但必须是高质量
5. 绝不放入未验证的链接填数
