# anything-roadmap · roadmap.html 模板设计提示词

> **用途**：把本提示词（整份）连同当前的 `references/template.html` 交给 AI（推荐带浏览器实测能力的 Claude Code），用于对 HTML 模板做全新重设计或迭代。所有改动必须落在下面共识框架内；框架外的发挥先提方案再动手。
>
> **来源**：本文由仓库 `CONSENSUS.md`（决策正本）§7/§8 派生。模板变了先改 CONSENSUS，再同步本文。

---

你是一位有主见的前端设计负责人，现在要为 **anything-roadmap** 重做/迭代它的输出模板 `template.html`。这是一张"任意领域学习路线图"的成品页面——每次运行 skill，只替换其中一段数据对象，就得到某个领域的学习地图。请严格按下面的约束与基线工作。

## 1. 主题与页面职责（设计的根）

- **主题**：某一领域的学习路线图——roadmap 的本义就是地图。当前隐喻是**勘察制图 / 领地地图（Field Atlas）**：模块=不规则地物轮廓的"领地"、模块关系=虚线路线+途径点、底布=经纬网格+罗盘。
- **读者**：刚拿到这份路线的学习者（多为技术背景）。
- **页面唯一职责**：先让读者一眼看清整个领域的版图（全局地图），再下钻任意模块找到"学什么、去哪学"（逐条验证过的来源）。
- **签名元素只有一个**：可点击的领地地图（悬停高亮关联路线并淡出其余、点击/键盘下钻）。大胆只花在这一处，其余保持安静克制。

## 2. 硬约束（违反任何一条即返工）

1. **单文件自包含**：全部 CSS/JS 内联，**零 CDN**——不引任何外部 script / stylesheet / 字体 / 图片。产出必须离线双击可开。
2. **DATA 契约不可改**：`<script>` 内 `/* DATA-START */` 到 `/* DATA-END */` 之间是一个 `const DATA = {...}` 对象（schema 见第 3 节）。渲染引擎读它生成整页；生成流程只替换这一段。**改视觉不许动 schema，动 schema 必须先升级 CONSENSUS 并说明迁移方案**。
3. **拼接坑**：模板顶部 HTML 注释里**不得出现**字面量 `/* DATA-START */` 或 `/* DATA-END */`（否则数据会被误注入注释区）。拼接锚点以 `<script>` 之后的标记为准。
4. **确定性渲染**：不用 `Math.random()` / `new Date()` 之类不稳定因素——领地形状、排布全部由模块序号经确定函数（黄金角螺旋 + 多频正弦径向扰动）派生，同一数据永远渲染同一张图。
5. **打印友好**：`@media print` 自动展开全部折叠、转浅色主题、`break-inside:avoid` 防截断。
6. **无障碍质量线**：焦点可见（`:focus-visible`）、地图与全部交互可键盘操作、`prefers-reduced-motion` 时关闭动效、文本对比度达标（最小字号文本 ≥4.5:1）。
7. **自检命令必须通过**：`grep -E 'src="http|link[^>]*href="http' roadmap.html` 为空；`const DATA` 全文恰好出现 1 次。

## 3. DATA 契约（只读参考）

```
DATA = {
  domain, date, mode ("prior"|"docs"), level,
  stats: { found, passed, dropped },
  overview: { landscape, cohesion },          // 两段导语
  relations: [{ from, to, note }],            // 模块序号从 1 起
  modules: [{
    name, why, refs: [priorPaths 索引],
    // 层级自适应（2-5 级）：模块可直挂 points（二级）；topics 可直挂 points（三级）；
    // chapters 可直挂 points（四级，默认）；经 sections（五级）。任一中间层可省，
    // 中间层必须聚合 ≥2 个子节点。sources: { type, lang, title, url, note }
    topics?:   [{ name, chapters?: [{ name, sections?: [{ name, points }],
                points? }], points? }],
    points?:   [...],
    exercises: [string]
  }],
  priorPaths: [{ tier, title, url, comment }]
}
```

## 4. 视觉系统 token（当前定稿；迭代保持气质，换方向需用户确认）

- **调色**（暗色科技感，暖调、离开深空蓝/纯黑+荧光的 AI 默认脸）：
  - 底 `--bg:#0e1217`（暖石墨）、面板 `#141a20` / `#1a222a`
  - 墨色三级：`--ink:#e9e5d9`（象牙白）→ `--dim:#a9a396` → `--faint`（须过对比度门槛）
  - 主强调 `--brass:#c9a36a`（铜金，结构与领地专用）
  - 四媒介=制图墨色：doc `#6ea8d8` 钢蓝 / blog `#d8b46a` 铜金 / video `#d87a6a` 陶土 / site `#8ab09a` 鼠尾草
- **字体**（纯系统栈）：标题 Georgia 衬线、只用于一两处；数据/标签/eyebrow 等宽（ui-monospace 系）；正文无衬线（system-ui 系）。中文标题行高按 CJK 基线（≈1.3–1.4），字距不滥用。
- **文案系统**：勘察语气贯穿——页眉=仪器读数（勘测日期/制图模式/来源验证）、工具栏=图层、练习=勘测练习、附录=参考图源、页脚=勘测说明。主动语态、面向读者、不卖弄。
- **氛围细节（克制）**：固定扫描线+暗角（pointer-events 关闭）、图框 HUD 角标、十字准星随鼠标+坐标读数。

## 5. 页面结构（区块可演进，职责不可丢）

页眉（eyebrow + 领域名 + 读数 chips）→ 粘性工具栏 → ①全局地图（领域全貌/拆分依据双面板 + 领地地图 + 图例 + 关系清单）→ ②学习路线（层级自适应 2-5 级折叠树 + 每模块勘测练习）→ ③参考图源（前人路线表）→ 勘测说明（colophon，含验证统计与生成声明）。

## 6. 交互基线（v2 能力清单——只能增不能减）

- **锚点跳转统一走 jumpTo**：自动展开目标、避开粘性工具栏遮挡、落点高亮；覆盖开页带 hash、前进/后退、地图领地点击三条入口。
- **媒介筛选**：DOC/BLOG/VID/SITE 四开关，实时"来源 n/m"计数；筛空的知识点行降透明度并给出提示。
- **下钻深度组**：收起 / 模块 / 主题 / 全部 四档。
- **地图 ↔ 大纲互指**：模块摘要带关联 ⇄Mn 芯片；关系清单条目可点击并悬停点亮地图对应路线。
- **模块跳转**：工具栏 M1..Mn 锚点，当前区块高亮 + 面包屑。
- 展开全部/收起全部；打印前自动展开。

## 7. 工作流程与验收

1. **先出设计计划再写码**：token（色/字/布局/签名）一段话方案，自查"是否像任意类似需求都会产出的 AI 默认脸"（奶油底+衬线+陶土、纯黑+酸性荧光、报纸 hairline 三件套均属默认脸）——像就改。
2. 写码注意 CSS 选择器特异性，避免区块间 padding/margin 互相抵消。
3. **浏览器实测**（有条件时必做）：渲染零 console 报错；领地/路线/途径点数量与 DATA 对账；悬停高亮、点击下钻 hash、四开关筛选+计数、深度组逐档、打印预览转浅色逐项过。
4. **回归底线**：换一份不同的 DATA 重渲染不报错；`console.log` 自检行输出模块数/知识点数。

## 8. 红线（不要做）

- ❌ 改 DATA schema 或把渲染逻辑写死具体领域的数据
- ❌ 引入任何外部资源或需要网络的特性（埋点、CDN、webfont、头像）
- ❌ 动 `roadmap.md` 的生成逻辑（模板只管 HTML）
- ❌ 加时间表/打卡/进度追踪类 UI（共识明确不做）
- ❌ 一次堆多个大胆元素——签名只有一个，其余做减法

## 9. 交付

- 产出完整 `template.html` 单文件（DATA 区保留示例数据即可）。
- 附一段变更说明：改了什么、为什么、验收结果（自检命令 + 浏览器实测清单）。
- 若涉及视觉方向或契约级变更：先更新 `CONSENSUS.md` §8 再交付。
